<section class="quiz" data-title="{{ quiz.label }}" data-subtitle="{{ quiz.desc }}">
    {% for question in questions %}
    <section class="question">
        <section class="text">
            {{ question.label }}
        </section>
        {% for option in question.options %}
        <section class="answer" {% if option.answer %} data-correct {% endif %}>{{ option.value }}</section>
        {% endfor %}
        {% if question.desc != null %}
        <section class="description">{{ question.desc }}</section>
        {% endif %}
        {% if question.hint != null %}
        <section class="hint">{{ question.hint }}</section>
        {% endif %}
    </section>
    {% endfor %}
</section>
